// exibe uma mensagem no console.
// show a console message
console.log('Teste');

// entendendo melhor o uso do console
// understanding the console usage
const x = 1;
const y = 3;

// ao criar uma função e dar Run/Salvar não irá exibir nada, pois não esta sendo pedido para ser exibido via console
// after create a function and run it, this will not be displayed, because we dont have a console.log function 
const soma = x + y;

// precisamos adicionar a função console.log para exibir o resultado
// we need to add console.log function to display the result

console.log('O resultado é: ', soma);